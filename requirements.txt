# Neuro Guard daemon (Google Calendar). Used by repo .venv — see scripts/daemon.sh
google-api-python-client>=2.0.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.0.0
# Menu bar tray (LaunchAgent com.neuroguard.tray) — see scripts/tray.sh
rumps>=0.4.0
